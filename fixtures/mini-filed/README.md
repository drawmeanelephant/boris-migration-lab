# Synthetic Filed.fyi first-slice fixture

Public, redistributable stand-in for the observed bounded source layout: one
`src/content/docs/changelog` record and three `src/content/docs/releases`
records. It is not a Filed.fyi export and contains no production content. One
record includes a clearly delimited untrusted directive-shaped block so the
converter's stripping/reporting path is covered without retaining its payload.
