# adversarial-astro fixture

Compact hostile source-shaped Astro tree: Unicode path/slug, encoded broken
links, raw MDX component syntax, and two dynamic routes matching one collection.
The scanner must preserve every input path and report ambiguity rather than
selecting a route silently.
