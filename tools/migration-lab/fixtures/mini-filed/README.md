# Synthetic Filed.fyi first-slice fixture

Public, redistributable stand-in for the observed bounded source layout: one
`src/content/changelog` record and three `src/content/releases` records. It is
not a Filed.fyi export and contains no production content.
