# Astro import-plan schema fixture

This minimal source produces the committed valid snapshot and plan payloads
under `schema-validation/fixtures/valid/`. The migration-lab test suite proves
those payloads still match current runtime emission before Ajv validates them.
