# Mini WordPress block-theme fixture

This synthetic fixture exercises only the `wordpress-theme` laboratory's
static FSE archaeology surface. `theme.json` and direct `templates/*.html` are
inventory evidence; they are not parsed, rendered, executed, or converted.
